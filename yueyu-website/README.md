# 青岛悦途国际经济技术合作有限公司网站

基于 Vue3 + Node.js + MySQL 构建的国际劳务合作网站。

## 项目特点

- 🚀 **现代化技术栈**: Vue3 + Vite + Node.js + MySQL
- 📱 **响应式设计**: 适配 PC、平板、手机等多种设备
- 🛡️ **安全可靠**: 表单验证、SQL注入防护、XSS防护、请求频率限制
- 🎨 **美观界面**: 基于 Element Plus 组件库，界面美观专业
- 📊 **管理后台**: 完整的数据管理、筛选、导出功能

## 功能模块

### 前台功能
- 🏠 首页展示（轮播图、服务项目、成功案例、新闻资讯）
- ℹ️ 关于我们（公司介绍、发展历程、企业文化）
- 🛠️ 服务项目（六大服务项目详细介绍）
- 🏆 成功案例（按国家筛选的案例展示）
- 📰 新闻资讯（行业动态、公司新闻）
- 📖 出国指南（分类指导信息）
- 🤝 合作伙伴（全球合作伙伴展示）
- 📞 联系我们（在线咨询表单、联系方式）

### 后台管理
- 🔐 管理员登录认证
- 📋 联系表单数据管理
- 🔍 数据筛选搜索（按姓名、电话、时间范围）
- 📊 数据排序分页
- 📥 Excel数据导出
- 🗑️ 数据删除功能

## 技术架构

### 前端技术栈
- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 现代化前端构建工具
- **Vue Router** - 官方路由管理器
- **Pinia** - 状态管理库
- **Element Plus** - Vue 3 UI 组件库
- **Axios** - HTTP 请求库
- **Day.js** - 轻量级日期处理库

### 后端技术栈
- **Node.js** - JavaScript 运行时
- **Express** - Web 应用框架
- **MySQL** - 关系型数据库
- **JWT** - 身份验证
- **Bcrypt** - 密码加密
- **Express-validator** - 数据验证
- **Helmet** - 安全中间件
- **Rate-limit** - 请求频率限制
- **XLSX** - Excel 文件处理

## 项目结构

```
yueyu-website/
├── frontend/                 # Vue3 前端项目
│   ├── src/
│   │   ├── components/      # 公共组件
│   │   ├── views/          # 页面组件
│   │   ├── router/         # 路由配置
│   │   ├── api/           # API 接口
│   │   ├── stores/        # 状态管理
│   │   └── styles/        # 样式文件
│   ├── package.json
│   └── vite.config.js
├── backend/                  # Node.js 后端项目
│   ├── config/            # 配置文件
│   ├── routes/            # 路由处理
│   ├── middleware/        # 中间件
│   ├── utils/             # 工具函数
│   └── server.js          # 入口文件
├── database/                # 数据库相关
│   └── init.sql           # 数据库初始化脚本
└── README.md
```

## 快速开始

### 环境要求
- Node.js >= 16.0.0
- MySQL >= 8.0
- npm 或 yarn

### 1. 克隆项目
```bash
git clone <repository-url>
cd yueyu-website
```

### 2. 数据库设置
```bash
# 登录 MySQL
mysql -u root -p

# 创建数据库和导入数据
source database/init.sql
```

### 3. 后端配置
```bash
cd backend

# 安装依赖
npm install

# 复制环境配置文件
cp .env.example .env

# 编辑 .env 文件，配置数据库连接和JWT密钥
# DB_HOST=localhost
# DB_USER=root
# DB_PASSWORD=your_password
# DB_NAME=yueyu_website
# JWT_SECRET=your_jwt_secret_key

# 启动后端服务
npm run dev
```

### 4. 前端配置
```bash
cd frontend

# 安装依赖
npm install

# 启动前端服务
npm run dev
```

### 5. 访问应用
- 前台地址: http://localhost:3000
- 后台地址: http://localhost:3000/admin/login
- API接口: http://localhost:5000

## 管理后台登录

- **用户名**: admin
- **密码**: admin123

> ⚠️ **安全提醒**: 部署时请修改默认密码！

## API 接口文档

### 公共接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/contact` | 提交联系表单 |
| GET | `/api/services` | 获取服务项目列表 |
| GET | `/api/services/:id` | 获取服务项目详情 |
| GET | `/api/cases` | 获取成功案例列表 |
| GET | `/api/cases/:id` | 获取案例详情 |
| GET | `/api/news` | 获取新闻资讯列表 |
| GET | `/api/news/:id` | 获取新闻详情 |
| GET | `/api/guides` | 获取出国指南列表 |
| GET | `/api/guides/:id` | 获取指南详情 |
| GET | `/api/partners` | 获取合作伙伴列表 |

### 管理后台接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/login` | 管理员登录 |
| GET | `/api/admin/contacts` | 获取联系表单数据 |
| DELETE | `/api/admin/contacts/:id` | 删除联系记录 |
| GET | `/api/admin/contacts/export` | 导出数据 |

## 部署说明

### 生产环境部署

1. **服务器要求**
   - CentOS 7+ / Ubuntu 18.04+
   - Node.js 16+
   - MySQL 8.0+
   - Nginx

2. **数据库配置**
   ```bash
   # 导入生产数据库
   mysql -u root -p < database/init.sql
   ```

3. **后端部署**
   ```bash
   cd backend
   npm install --production
   pm2 start server.js --name yueyu-api
   ```

4. **前端构建**
   ```bash
   cd frontend
   npm run build
   ```

5. **Nginx 配置示例**
   ```nginx
   server {
       listen 80;
       server_name yourdomain.com;
       
       # 前端静态文件
       location / {
           root /path/to/frontend/dist;
           try_files $uri $uri/ /index.html;
       }
       
       # API 代理
       location /api {
           proxy_pass http://localhost:5000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

### Docker 部署

```bash
# 构建镜像
docker-compose build

# 启动服务
docker-compose up -d
```

## 安全特性

- ✅ SQL 注入防护（参数化查询）
- ✅ XSS 攻击防护
- ✅ CSRF 攻击防护
- ✅ 请求频率限制
- ✅ 数据验证和过滤
- ✅ JWT 身份认证
- ✅ 密码加密存储
- ✅ 安全响应头设置

## 性能优化

- ⚡ Vue 3 Composition API
- ⚡ Vite 快速构建
- ⚡ 组件按需加载
- ⚡ 图片懒加载
- ⚡ API 响应缓存
- ⚡ Nginx Gzip 压缩

## 浏览器支持

- Chrome >= 88
- Firefox >= 78  
- Safari >= 14
- Edge >= 88

## 开发指南

### 添加新页面
1. 在 `frontend/src/views/` 创建页面组件
2. 在 `frontend/src/router/index.js` 添加路由
3. 在导航组件中添加链接

### 添加新 API
1. 在 `backend/routes/` 创建路由文件
2. 在 `frontend/src/api/` 添加 API 方法
3. 在页面组件中调用

## 许可证

MIT License

## 技术支持

如需技术支持或定制开发，请联系：
- 📧 Email: syndicate_2008@yahoo.com
- 📞 Phone: 0532-81978007

---

© 2025 青岛悦途国际经济技术合作有限公司 版权所有